version: '3.8'

services:
  whatsapp-bot:
    build: .
    volumes:
      - ../sessions:/app/sessions
      - ../storage:/app/storage
      - ../logs:/app/logs
    restart: unless-stopped
    environment:
      - NODE_ENV=production
